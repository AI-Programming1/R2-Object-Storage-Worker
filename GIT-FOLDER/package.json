{
  "name": "video-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare R2 Video Hosting Worker",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "db": "wrangler d1 execute DB --local",
    "db-remote": "wrangler d1 execute DB --remote",
    "cron": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^4.25.0"
  }
}